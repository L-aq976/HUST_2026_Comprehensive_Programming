# 网络流量分析系统

这是一个基于Flask Web界面和C++后端的网络流量分析系统，用于分析网络流量数据并提供多种分析功能。

## 项目结构
源代码/ ├── web_app/ # Flask Web应用 │ ├── templates/ # HTML模板 │ │ └── simple_index.html │ ├── uploads/ # 上传文件存储目录 │ ├── init.py │ └── simple_app.py # 主应用文件 └── .venv/ # Python虚拟环境

## 功能特性

- **IP范围分析**: 分析指定IP范围内的网络流量
- **最短路径分析**: 基于拥塞程度的最短路径查找
- **星型结构分析**: 识别网络中的星型拓扑结构
- **流量统计**: 统计各类流量数据
- **HTTPS流量分析**: 专门分析HTTPS流量
- **单向流量分析**: 识别单向通信模式
- **图信息展示**: 显示网络图的基本信息

## 部署要求

### 系统要求
- Windows操作系统
- Python 3.8+
- C++编译器（用于编译后端程序）
- 现代Web浏览器

### Python依赖
- Flask 2.0+
- 其他标准库：os, subprocess, threading, queue, time

## 部署步骤

### 1. 环境准备

确保系统已安装Python 3.8或更高版本：

```bash
python --version
```

### 2. 激活虚拟环境

项目已包含虚拟环境，直接激活即可：

```bash
# 进入项目目录
cd c:\Users\32862\Desktop\2026程序设计综合课程\源代码

# 激活虚拟环境（Windows）
.venv\Scripts\activate
```

### 3. 安装Python依赖

虚拟环境中已包含所需依赖，如需重新安装：

```bash
pip install flask
```

### 4. 编译C++后端程序

**重要**: 需要先编译C++分析程序，生成可执行文件 `main.exe` 并放置在项目根目录或 `bin/` 目录下。

编译步骤：
1. 确保有C++源代码文件（.cpp文件）
2. 使用合适的编译器（如g++、MinGW、Visual Studio）编译
3. 生成的可执行文件命名为 `main.exe`
4. 放置在项目根目录或 `bin/` 目录下

### 5. 启动Web应用

```bash
# 进入web_app目录
cd web_app

# 启动Flask应用
python simple_app.py
```

应用默认运行在 `http://127.0.0.1:5000`

### 6. 访问应用

在浏览器中打开：`http://127.0.0.1:5000`

## 使用说明

### 数据上传
1. 点击"选择文件"按钮上传CSV格式的网络流量数据
2. 系统会自动启动C++分析程序处理数据
3. 上传成功后即可使用各种分析功能

### 分析功能
- **IP范围分析**: 输入三个IP地址，分析该范围内的流量模式
- **最短路径分析**: 输入源IP和目标IP，查找基于拥塞程度的最短路径
- **星型结构分析**: 自动识别网络中的星型拓扑
- **流量统计**: 统计整体流量情况
- **HTTPS分析**: 专门分析HTTPS流量特征
- **单向流量**: 识别单向通信模式
- **图信息**: 显示网络图的基本统计信息

## 故障排除

### 常见问题

1. **C++程序未找到**
   - 确保 `main.exe` 文件存在于项目根目录或 `bin/` 目录
   - 检查C++程序编译是否正确

2. **文件上传失败**
   - 确保上传的是CSV格式文件
   - 检查uploads目录权限

3. **分析功能无响应**
   - 检查C++程序是否正常运行
   - 查看控制台输出获取详细错误信息

### 日志查看
应用运行时会输出详细日志，包括：
- C++程序启动状态
- 命令执行结果
- 错误信息

## 开发说明

### 项目架构
- **前端**: HTML + JavaScript (通过Flask模板渲染)
- **后端**: Python Flask框架
- **分析引擎**: C++程序，通过子进程调用

### 扩展功能
可以添加新的分析功能：
1. 在C++程序中实现新的分析算法
2. 在 `simple_app.py` 中添加对应的API路由
3. 在前端页面添加相应的交互界面

## 许可证

本项目仅供教学和研究使用。

## 技术支持

如遇部署或使用问题，请检查：
1. Python环境配置
2. C++程序编译和放置位置
3. 文件上传格式和权限