# 网络流量分析系统（用户指南）

这是一个面向网络流量数据的分析工具：
1. 后端分析引擎使用 C++，负责高性能计算。
2. Web 界面使用 Flask，负责上传数据、发起分析、展示结果。
3. 支持双页面启动：基础分析页面 + 拓扑可视化页面。

如果你是第一次使用，建议按下面的 快速开始 直接操作。

## 你可以做什么

1. 上传 CSV 流量数据并进行分析。
2. 进行 IP 范围检查、最短路径分析（拥塞/跳数）、星型结构识别。
3. 查看 HTTPS 统计、单向流量统计、图基础信息。
4. 按指定中心 IP 生成网络拓扑可视化页面。

## 快速开始（推荐）

### 第 1 步：准备环境

1. 操作系统：Windows。
2. Python：建议 3.10 及以上。
3. C++ 编译器：支持 C++17 的 g++（如 MinGW-w64）。

在项目根目录打开 PowerShell 后，可先检查 Python：

```powershell
python --version
```

### 第 2 步：安装 Python 依赖

如果项目中已有 .venv，可激活后安装：

```powershell
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

如果你没有虚拟环境，也可以先创建：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

### 第 3 步：编译 C++ 程序

Web 页面会调用 C++ 可执行文件进行分析，因此必须先生成 main.exe。

方式 A（推荐，VS Code 任务）：
1. 在 VS Code 中运行任务 Build C++ Project。
2. 成功后应看到 bin/main.exe。

方式 B（命令行自行编译，示例）：

```powershell
g++ -std=c++17 -I.\include .\src\main.cpp .\src\handle_data.cpp .\src\path_finder.cpp .\src\sort_filter.cpp .\src\safe.cpp .\src\union_find_external.cpp -o .\bin\main.exe
```

### 第 4 步：启动系统

一键启动双页面（推荐）：

```powershell
.\start.bat
```

启动后会打开两个地址：
1. http://127.0.0.1:5000（基础分析页面）
2. http://127.0.0.1:5001（可视化页面）

## 数据准备

### 方式 1：直接使用 CSV

将 CSV 文件准备好后，在页面上传即可。

CSV 头部格式应为：

```text
Source,Destination,Protocol,SrcPort,DstPort,DataSize,Duration
```

### 方式 2：从 PCAP 转换

项目内置了转换脚本，可以把 data 目录中的 pcap/pcapng/cap 转为 CSV：

```powershell
python .\convert_pcap.py
```

强制重新转换：

```powershell
python .\convert_pcap.py --force
```

## 页面使用说明

### 基础分析页面（5000）

1. 先上传 CSV 文件。
2. 上传成功后，C++ 分析引擎会自动启动。
3. 根据页面按钮执行分析功能：
   1. IP 范围分析
   2. 最短路径（拥塞）
   3. 最短路径（跳数）
   4. 星型结构分析
   5. 流量统计
   6. HTTPS 统计
   7. 单向流量统计
   8. 图信息

### 可视化页面（5001）

1. 上传 CSV。
2. 输入目标 IP。
3. 系统调用 C++ 提取子图并生成可视化结果（HTML/图片）。

## 常见问题

### 1. 提示找不到 C++ 分析程序

检查以下内容：
1. 是否已成功生成 bin/main.exe。
2. 是否在项目根目录启动程序。
3. 如果程序正在运行，重编译可能失败（常见为 main.exe 被占用）。先关闭运行中的页面或相关进程，再重新编译。

### 2. 编译时报 Permission denied（main.exe）

这是可执行文件被占用导致的常见情况：
1. 关闭由 start.bat 打开的服务窗口。
2. 结束可能占用 main.exe 的进程。
3. 重新运行 Build C++ Project。

### 3. 上传失败或分析无响应

1. 确认上传的是 CSV 文件，且列头格式正确。
2. 查看启动窗口日志，确认 C++ 程序是否正常启动。
3. 确认端口 5000/5001 未被其他程序占用。

## 目录说明（使用者关心）

1. data：原始与转换后的数据文件。
2. bin：C++ 编译产物（main.exe）。
3. web_app：Web 服务代码与模板。
4. start.bat：一键启动入口。
5. convert_pcap.py：PCAP 转 CSV 工具。

## 结束与重启

1. 关闭 start.bat 打开的命令行窗口即可停止服务。
2. 需要重启时，重新执行 start.bat。

## 说明

本项目主要用于教学与实验场景。